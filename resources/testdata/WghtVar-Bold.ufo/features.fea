# aalt lookups are supposed to go "at the front" but it goes after rvrn
feature aalt {
    feature salt;
    sub plus by bar;
} aalt;


# salt lookup should end up at the end
feature salt {
    sub bar by plus;
} salt;
