[package]
name = "fontdrasil"
version = "0.0.1"
edition = "2021"
license = "MIT/Apache-2.0"
description = "Common aspects of FE and BE font compilation; the world tree that connects all layers"
repository = "https://github.com/googlefonts/fontmake-rs"
readme = "README.md"
categories = ["text-processing", "parsing", "graphics"]

[features]

[dependencies]
ordered-float.workspace = true
smol_str.workspace = true
serde.workspace = true
write-fonts.workspace = true
phf = { version = "0.11" }

[dev-dependencies]
diff.workspace = true
ansi_term.workspace = true
tempfile.workspace = true
phf_codegen = "0.11.1"

# this is an 'example' so that we can use dev-dependencies; it is run while
# rebuilding our glyph data. See glyphdata/README.md
[[example]]
name = "run_phf_codegen"
path = "glyphdata/run_phf_codegen.rs"

